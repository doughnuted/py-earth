import numpy as np
FLOAT = np.float64
# ``np.int`` was removed in recent versions of NumPy. ``np.int_`` is the
# supported replacement that maps to the platform default integer type.
INT = np.int_
INDEX = np.intp
BOOL = np.uint8
